import 'package:flutter/widgets.dart';

class SampleWidegt extends StatelessWidget {
  const SampleWidegt({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
